# Local paths are resolved from the repository root.
DATABASE_PATH=backend/data/dashboard.db
SOURCE_DATA_DIR=data
FRONTEND_ORIGIN=http://localhost:5173

# Use "mock" until a compatible model key is configured.
AI_PROVIDER=mock
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=

