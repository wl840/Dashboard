# AI 使用说明

## 使用了什么

本项目使用 OpenAI Codex 协助：

- 读取作业说明并拆分 24 小时优先级
- 对三张 CSV 做只读数据质量审计
- 生成 FastAPI、React 和测试代码初稿
- 根据测试、类型检查和构建输出迭代修复
- 编写交付文档与演示脚本

AI 没有被用来生成最终经营数字。最终数字来自 SQLite 查询，测试直接对数据库结果断言。

## 如何拆任务

1. 先审计数据，冻结指标口径。
2. 建立 raw、canonical、quality 三层数据结构。
3. 实现唯一的 Analytics Repository。
4. 让看板 API 和 AI 工具复用该 Repository。
5. 再实现 UI、答案证据卡和图表联动。
6. 最后通过自动化测试、类型检查和生产构建验收。

一个真实使用过的提示方向：

> 只读分析 sales.csv、stores.csv、products.csv。统计行数、完全重复、重复 order_id 的冲突、日期格式、金额缺失和币种符号、非正数量/金额、门店与商品脏外键；不要修改源文件，并输出可用于自动化测试的基线。

## AI 哪里出了错，如何发现和修复

### 1. 日期格式判断不完整

第一次分析只覆盖 YYYY-MM-DD 和 YYYY/MM/DD，一度把 75 条 DD-MM-YYYY 记录视为不可解析。随后输出全部异常原值，发现第三种稳定格式；补充解析后确认 12,131 条日期全部可解析。

修复：

- 导入器只允许三个明确格式，不使用宽松、依赖区域设置的自动解析。
- 测试断言规范订单数为 12,051，避免格式遗漏造成静默丢数。

### 2. 重复订单不能全部按“完全重复”处理

初步总数显示 80 个重复订单号，但逐组比较发现只有 76 组完全重复；另外 4 组包含日期格式、数量或门店冲突。

修复：

- 先规范化再比较。
- 数量冲突优先选择 amount = qty × unit_price 的记录。
- 门店仍有冲突时归入未知，不让 AI 猜归属。
- 每次决策写入 data_quality_issues。

### 3. 前端分包规则误匹配

为了消除大包提示，初版规则使用 id.includes("react")，误把 lucide-react 分到 React vendor，生产构建立即失败。

修复：

- 将规则收紧为 react、react-dom 和 scheduler 的精确 node_modules 路径。
- 重新运行类型检查和生产构建；最终入口、React、图表三类 chunk 均正常生成。

## 如何防止 AI 编造数字

- 模型只能调用四个 allowlist 工具，不能提交 SQL。
- 参数由 FastAPI 做类型和日期校验。
- 工具结果直接来自 Analytics Repository。
- 最终答案中的金额、订单数和百分比由服务端模板填充。
- 响应附带 evidence：工具、参数、结果、覆盖率和指标口径。
- 模型无工具调用、商品不存在或问题超出字段范围时明确拒答。
- 自动化测试比较 AI evidence 与数据库基线。

因此，即使模型规划错误，最坏结果也是查询错误维度或拒答，不会凭空生成数据库中不存在的数字。

## 哪些部分坚持人工判断

- “营业额、订单数、客单价”的业务定义。
- 没有退款字段时如何处理负数记录。
- 冲突门店不随意选择的原则。
- 未知外键保留金额、维度归入未知的决定。
- 金额使用整数分而非浮点数。
- UI 信息层级、证据卡展示内容和交付优先级。

这些选择涉及业务责任和可解释性，不能仅以“代码能跑”作为判断依据。
